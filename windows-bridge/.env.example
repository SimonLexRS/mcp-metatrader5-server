# Windows MT5 Bridge Server Configuration
# This server runs on Windows and exposes MT5 via HTTP for remote access

# Bridge Server Configuration
MT5_BRIDGE_HOST=0.0.0.0
MT5_BRIDGE_PORT=5555

# Authentication Token
# This should match the MT5_BRIDGE_TOKEN on the Linux server
# Generate with: openssl rand -base64 32
AUTH_TOKEN=your-secure-token-here

# MetaTrader 5 Configuration
MT5_PATH=C:\Program Files\MetaTrader 5\terminal64.exe
MT5_LOGIN=123456
MT5_PASSWORD=your_password
MT5_SERVER=YourBroker-Demo

# Auto-connect to MT5 on startup
MT5_AUTO_CONNECT=true
